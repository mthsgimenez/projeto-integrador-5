{$DOMAIN_NAME::80} {
    root * /usr/share/caddy/html

    handle /api/* {
        reverse_proxy server:3000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}
